---
name: natural-korean-skills
description: "한국어로 설명하거나 문서를 쓸 때 번역투와 AI 문체를 걷어내고 자연스러운 한국어로 쓰게 합니다. 두 모드로 나뉩니다. 일반 모드는 일상 설명, 대화, 안내문에, 연구 모드는 AI 연구와 기술 문서에 씁니다. 연구 모드에서는 attention, fine-tuning, baseline 같은 AI 용어를 영문 그대로 두고 문장 골격만 한국어로 씁니다. 트리거: 자연스러운 한국어로, 번역투 빼고, 한국어로 설명해줘, 한국어 문서 작성, 논문 정리, 연구 노트, 기술 블로그, natural Korean writing, Korean tech writing. 맞춤법과 띄어쓰기 교정 자체가 목적이거나 시, 가사, 광고 카피처럼 목소리가 목적인 글에는 쓰지 않습니다."
version: 0.1.0
---

# 자연스러운 한국어 쓰기 (natural-korean-skills)

LLM이 쓰는 한국어는 대개 문법이 틀리지 않습니다. 대신 영어 통사 구조 위에 한국어 어휘를 얹은 문장이 됩니다. "이러한 변화는 효율성의 향상을 가져올 수 있습니다"는 어디 하나 틀린 데가 없지만, 한국어를 모국어로 쓰는 사람은 이렇게 쓰지 않습니다. "이렇게 바꾸면 효율이 올라갑니다"라고 씁니다.

맞춤법 검사기는 이런 문장을 잡지 못합니다. 틀린 데가 없기 때문입니다. 문제는 문장의 골격이고, 골격은 쓰고 나서 고치는 것보다 쓰면서 잡는 편이 쌉니다. 이 스킬은 그래서 쓰기 **전**에 적용하는 규칙입니다.

규칙은 기계적입니다. 어느 어절이 규칙을 어겼는지 손으로 짚을 수 있고, 취향 문제로 넘어가지 않습니다.

## 언제 쓰나

- 한국어로 개념을 설명하거나 답변할 때. 쓰기 **전**에 적용하는 스킬입니다.
- 한국어 문서, README, 기술 블로그, 연구 노트, 회의록을 쓸 때.
- 영어 자료를 읽고 한국어로 정리할 때. 번역투가 가장 많이 새어 들어오는 자리입니다.
- 이미 쓴 한국어가 어딘가 뻣뻣하게 읽힐 때.

## 언제 쓰지 않나

- 맞춤법과 띄어쓰기 교정이 목적일 때. 이 스킬은 철자가 아니라 문장 골격을 봅니다.
- 완성된 글에 점수나 등급을 매기는 게 목적일 때. 여기서는 진단하지 않고 고쳐 씁니다.
- 한국어가 아닌 글을 다룰 때.
- 시, 가사, 광고 카피처럼 목소리 자체가 목적인 글. 규칙이 개성을 깎아냅니다.

## 두 가지 모드

먼저 모드를 정합니다. 사용자가 말하지 않으면 글의 종류를 보고 고르고, **고른 모드는 말하지 않습니다**. 사용자가 물으면 그때 답합니다.

| | 일반 모드 | 연구 모드 |
|---|---|---|
| 독자 | 동료, 친구, 일반 사용자 | AI 연구자, 엔지니어 |
| 쓰는 글 | 대화 답변, 안내문, 개념 설명, 블로그 | 논문 정리, 실험 노트, 기술 문서, 코드 리뷰 |
| 영문 용어 | 꼭 필요할 때만. 한 번 쓰고 한국어로 풀어 줌 | AI 기술 용어는 영문 그대로. 아래 용어 정책 참조 |
| 어투 | 사용자가 쓴 어투를 따라감(해요체/합니다체/반말) | 문서면 `-다`체, 답변이면 합니다체 |
| 추측 표현 | 줄임. "~할 수 있습니다"를 단언으로 | **살림**. 불확실성은 연구 글에서 정보다 |
| 수치와 출처 | 있으면 그대로 | 그대로. 없는 수치를 만들지 않음 |
| 문장 길이 | 평균 12~15어절, 최대 25어절 | 평균 15~18어절, 최대 30어절 |

추측 표현 항목이 두 모드의 가장 큰 차이입니다. 일반 모드에서 "성능이 개선될 수 있을 것으로 보입니다"는 자신 없는 말투로 읽히므로 "성능이 좋아집니다"로 단언합니다. 연구 모드에서 같은 문장을 단언으로 바꾸면 실험이 보여주지 않은 것을 주장하게 됩니다. 이때는 "~로 보인다"를 그대로 둡니다. **어느 모드든, 원문이 가진 확신의 세기를 바꾸면 그건 다듬기가 아니라 다른 주장입니다.**

## 두 모드에 공통인 규칙

### 반드시 고치는 것

어긴 자리를 어절 단위로 짚을 수 있는 규칙입니다. 판단이 들어가지 않습니다.

| 규칙 | 이렇게 | 이렇게 말고 |
|---|---|---|
| 이중 피동 금지 | 분석된다 / 보인다 / 나뉜다 | 분석되어진다 / 보여진다 / 나뉘어진다 |
| `~에 있어서` 금지 | 교육에서는 / 이 경우 | 교육에 있어서 / 이 사안에 있어서 |
| `가지고 있다` 금지 | 기능이 다양하다 / 영향이 크다 | 다양한 기능을 가지고 있다 |
| 영어식 피동 환원 | 연구팀이 개발했다 | 연구팀에 의해 개발되었다 |
| 추상 주어 + 만능 동사 | 이렇게 바꾸면 속도가 빨라진다 | 이러한 변화는 속도 향상을 가져온다 |
| 명사화 풀기 | 정확도가 떨어진다 | 정확도의 하락이 발생한다 |
| AI 결말 관용구 삭제 | (그냥 본문으로 끝냄) | 결론적으로 / 시사하는 바가 크다 / 주목할 만하다 |
| 조사 번역투 분산 | ~를 다룬다 / ~로 측정한다 / ~하려고 | 한 단락에 `에 대해`, `를 통해`, `을 위해`가 3회 이상 |
| `~적 N` 체인 | 구조의 한계 / 전략 차원의 의미 | 구조적 한계 + 전략적 함의 + 본질적 변화 연달아 |
| 연결어미 뒤 쉼표 | ~하고 ~하며 ~한다 | ~하고, ~하며, ~한다 |
| 3박자 반복 | 항목 수를 2개나 4개로 섞음 | 모든 문장이 "A하고, B하며, C한다" |

### 판단이 필요한 것

규칙보다는 방향입니다. 지키되 기계적으로 세지 않습니다.

| 항목 | 방향 |
|---|---|
| 문장 리듬 | 긴 문장 다음에 짧은 문장. 길이가 모두 비슷하면 AI가 쓴 티가 납니다 |
| 주어 생략 | 문맥에서 자명한 주어는 뺍니다. "우리는", "이것은"을 문장마다 붙이지 않습니다 |
| 지시어 절제 | "이러한", "해당", "그것"이 한 단락에 서너 번 나오면 명사를 다시 씁니다 |
| 복수 `-들` | 한국어는 복수를 잘 안 붙입니다. "모델들을 학습" → "모델을 학습" |
| 한자어 낮추기 | 진행하다, 실시하다, 수행하다 → 하다. 단, 연구 모드의 정착 용어는 그대로 |
| 불릿 절제 | 대화 답변에서 항목 3개 이하면 문장으로 씁니다. 목록은 순서나 조건이 있을 때 |
| hype 어휘 | 혁신적, 압도적, 강력한은 한 단락에 하나까지. 나머지는 구체적 사실로 |

## 연구 모드: 용어 표기 정책

AI 용어를 영문으로 두는 이유는 멋이 아니라 검색과 대조입니다. `어텐션`으로 써 두면 논문의 attention과 이어 붙지 않고, 독자가 원문을 찾을 때 한 번 더 번역해야 합니다. 대신 조사와 어미, 문장 골격은 전부 한국어로 씁니다. 영문 용어를 박아 넣은 한국어 문장이지, 한국어 조사를 붙인 영어 문장이 아닙니다.

용어는 세 층으로 나뉩니다. 판단 기준은 하나입니다. **논문과 코드에서 그 형태로 쓰이는가, 아니면 한국어 사전에 이미 들어온 말인가.**

**1층 — 영문 그대로** (연구 모드에서만)
architecture와 기법 이름, 실험 용어입니다. transformer, attention, MoE, LoRA, RLHF, DPO, RAG, fine-tuning, pretraining, distillation, quantization, loss, gradient, optimizer, learning rate, batch size, checkpoint, overfitting, baseline, ablation, benchmark, zero-shot, few-shot, prompt, token, embedding, context window, hallucination, alignment, latency, throughput, SOTA. 약어와 고유명사(LLM, GPT, BERT, GPU, FLOPs, arXiv)도 여기 들어갑니다.

**2층 — 한글 음차** (두 모드 공통)
이미 한국어로 굳은 외래어입니다. 모델, 데이터, 파라미터, 레이어, 벡터, 알고리즘, 라이브러리, 메모리, 시퀀스, 노이즈, 샘플, 서버. 이 말들을 굳이 영문으로 되돌리지 않습니다. "model을 학습한다"가 아니라 "모델을 학습한다"입니다.

**3층 — 한국어 단어** (두 모드 공통)
학습, 추론, 성능, 정확도, 가중치, 수렴, 분포, 오차, 실험, 평가, 재현, 한계. 영어로 바꿀 이유가 없는 말들입니다.

전체 목록과 경계 사례는 `references/ai-term-policy.md`에 있습니다.

### 영문 용어에 조사 붙이기

한글로 읽었을 때의 받침을 기준으로 조사를 고릅니다.

- token(토큰) → **을/은/으로**: "token을 자른다"
- loss(로스) → **를/는/로**: "loss가 줄었다"
- LLM(엘엘엠) → **을/은/으로**: "LLM으로 요약한다"
- GPU(지피유) → **를/는/로**: "GPU를 두 장 썼다"

영어 어근에 `-하다`를 바로 붙이지 않습니다. 조사를 넣거나 한국어 동사를 씁니다.

- ✗ "fine-tuning했다" → ✓ "fine-tuning을 했다" / "fine-tuning으로 학습했다"
- ✗ "eval돌렸다" → ✓ "eval을 돌렸다"

### 같은 단어가 두 뜻일 때

기술 개념이면 영문, 일반적인 뜻이면 한국어로 씁니다.

- bias: 모델의 bias 항 / 데이터의 편향
- scale: scaling law / 규모가 크다
- attention: attention 층 / 주의를 기울이다

## 쓰기 전 점검 목록

문장을 쓰기 전에 이 일곱 가지를 봅니다. 전부 기계적으로 짚을 수 있습니다.

1. **이중 피동** — `되어지`, `보여지`, `나뉘어지`가 있으면 한 번만 피동으로 바꿉니다.
2. **영어 뼈대** — 주어가 추상명사이고 동사가 가져오다, 만들다, 제공하다면 구체적인 주어로 고쳐 씁니다.
3. **조사 쏠림** — 한 단락에서 `에 대해`, `를 통해`, `을 위해`를 세어 봅니다. 셋 다 두 번 넘으면 분산합니다.
4. **명사화** — `~의 향상`, `~의 발생`, `~을 실시`를 동사로 되돌립니다.
5. **마무리** — 마지막 문단이 "결론적으로", "시사하는 바", "~할 때다"로 끝나면 통째로 지웁니다.
6. **리듬** — 문장 길이가 다 비슷하면 하나를 짧게 자릅니다.
7. **용어** (연구 모드) — 음차한 AI 용어(어텐션, 파인튜닝, 베이스라인)를 영문으로 되돌립니다.

기계적인 1차 검사는 `scripts/ko-lint.py`로 돌립니다. stdin이나 파일을 받고, `--mode research`로 용어 정책까지 봅니다.

```bash
python3 scripts/ko-lint.py --mode research 초안.md
```

## 작업 절차

1. 모드를 고릅니다. 사용자가 말하지 않으면 글의 종류로 판단하고, 고른 모드는 말하지 않습니다.
2. 무슨 말을 해야 하는지 먼저 정합니다. 문체 규칙은 할 말이 있는 글에만 쓸모가 있습니다.
3. 한국어 어순으로 씁니다. 영어 문장을 머릿속에서 옮겨 적지 않습니다. 옮겨 적은 문장은 위 표의 왼쪽 열로 되돌릴 수 없습니다.
4. 다 쓰고 점검 목록 일곱 가지를 훑습니다. 긴 문서면 린터를 돌립니다.
5. 고칠 때 사실을 바꾸지 않습니다. 수치, 날짜, 고유명사, 인용문, 인과의 방향, 확신의 세기는 그대로 둡니다.
6. 이미 자연스러우면 손대지 않습니다. 고칠 것이 없다고 말합니다.

## 출력 형식

**기본값은 한국어 본문뿐입니다.** 사용자가 원하는 것은 결과물이지 분석이 아닙니다. 모드 선언, 규칙 이름, 위반 건수, 변경 요약을 붙이지 않습니다.

예외 하나. 5번에서 일부러 그대로 둔 표현이 있으면 본문 뒤에 한 줄을 붙입니다.

```
그대로 둠: "~에 의해 매개된다" — 학술 정형 표현이라 능동으로 바꾸면 뜻이 달라짐
```

사용자가 "뭘 고쳤어?", "before/after 보여줘", "어떤 규칙이야?"라고 물으면 그때 표를 냅니다.

```markdown
| 규칙 | 원문 | 고친 글 |
|---|---|---|
| 이중 피동 | 분석되어집니다 | 분석합니다 |
| ~에 있어서 | 교육에 있어서 | 교육에서는 |
```

## 경계

**하는 일**

- 번역투와 AI 문체를 걷어내고 한국어 어순으로 다시 씁니다.
- 연구 모드에서 AI 용어를 영문으로 두고 나머지를 한국어로 씁니다.
- 원문의 사실, 수치, 인용, 확신의 세기를 그대로 지킵니다.
- 일부러 고치지 않은 자리를 한 줄로 알립니다.

**하지 않는 일**

- 맞춤법과 띄어쓰기를 검사하지 않습니다. 철자가 아니라 문장 골격을 봅니다.
- 글에 점수나 등급을 매기지 않습니다. 진단 대신 고쳐 쓴 글을 냅니다.
- 내용이 빈 글을 채워 주지 않습니다. 할 말 없는 문단은 매끄럽고 짧은 빈 문단이 될 뿐입니다. 그럴 때는 다듬는 대신 그렇다고 말합니다.
- 없는 수치나 근거를 보태지 않습니다. 읽기 좋아지려고 원인, 빈도, 메커니즘을 지어내면 다듬기가 아닙니다.
- "~로 보인다"를 "~다"로 올리지 않습니다. 추측을 사실로 바꾸면 주장이 달라집니다.
- 학술 정형 표현, 법률 문구, 직접 인용, 코드, 고유명사는 건드리지 않습니다.
- 문장을 무조건 짧게 만들지 않습니다. 목표는 짧은 글이 아니라 한국어답게 읽히는 글입니다.

## 참고 자료

- `references/ai-term-policy.md` — AI 용어 3층 분류 전체 목록, 경계 사례, 조사 결합 규칙
- `examples/before-after.md` — 두 모드의 적용 예시와 과교정 사례
- `scripts/ko-lint.py` — 표준 라이브러리만 쓰는 기계적 검사기
